
const validate_inputs = function (
    param_username, 
    param_password)
{

    //

    const username = param_username

    const password = param_password

    //

}

const controller_kategorijos_create = function (req, res)
{
    
    // validate_inputs
    
    const username = req.body.username

    const password = req.body.password

    //

}

export default controller_kategorijos_create